---

---
<script is:inline>
      // ☝️ This script prevent the FART effect.
      if (localStorage.getItem("theme") === null) {
        document.documentElement.setAttribute("data-theme", "night");
      } else
        document.documentElement.setAttribute(
          "data-theme",
          localStorage.getItem("theme")
        );
      // "theme" LocalStorage value is set by the package to remember user preference.
      // The value is checked and applyed before rendering anything.
    </script>
    <script>
      import { themeChange } from 'theme-change';
      themeChange();
      // 👆 you could import the CDN directly instead of these two lines
    </script>
<nav id="navbar" class="pt-4">
<div class="flex flex-col gap-4 pt-4 px-4 w-max">
<div class="text-xl rounded-lg px-4">Tema</div>
  <button class="rounded-xl w-full btn glass" data-set-theme="corporate" data-act-class="ACTIVECLASS">
    <span class="text-2xl">
    🌞
    </span>
    Light
  </button>
  <button class="rounded-xl w-full btn glass" data-set-theme="dark" data-act-class="ACTIVECLASS">
    <span class="text-2xl">
    🌑
    </span>
    Dark
  </button>
  <button class="rounded-xl w-full btn glass" data-set-theme="night" data-act-class="ACTIVECLASS">
    <span class="text-2xl">
    🌙
    </span>
    Night
  </button>
</div>
</div>
</div>
    </nav>
