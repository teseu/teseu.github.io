---

const { classLink, link, caption  } = Astro.props
---

  <a href={link} class:list={["group link", classLink ]} >
    <slot name="img" /> 
    <div class="legend">{caption}</div>
  </a>

  <style>
    
    .link {
        @apply relative flex items-end overflow-hidden rounded-lg bg-gray-100 shadow-lg;
    }
    .img {
        @apply absolute inset-0 h-full w-full object-cover transition duration-200 saturate-50 hover:saturate-100 hover:scale-110;
    }
    
    .img-div {
        @apply pointer-events-none absolute inset-0 bg-gradient-to-t from-gray-800 via-transparent to-transparent opacity-50;
    }
    
    .legend {
        @apply relative w-full pl-4 pb-3 pt-12 text-lg text-white md:pl-5 md:text-2xl bg-gradient-to-t from-neutral-950/90 via-neutral-950/50 to-neutral-950/0;
    }
    </style>
